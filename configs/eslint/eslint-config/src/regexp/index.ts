import type { ESLintConfig } from "../typings"
import { createConfig } from "../create-config"

export const regexp = createConfig(async () => {
  const configs: ESLintConfig[] = []

  return configs;
})