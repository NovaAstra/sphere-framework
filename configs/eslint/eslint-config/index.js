const regex = /[0-9]/;

const foo = bar.map(element => unicorn(element)).flat();

const aas = parseInt('10', 2);

  // 禁止禁用 no-console 规则
console.log('Hello');

/* eslint-disable-line no-console */ // 未允许使用 eslint-disable-line
console.log('Hello');